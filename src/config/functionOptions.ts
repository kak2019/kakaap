
const functions = [
    "Buses",
    "GTA & JVs",
    "GTP",
    "Penta",
    "VCE",
];

const functionByOptions = functions.map((item)=>
(
    {
       key: item, 
       text: item
   }
));

export { functionByOptions };